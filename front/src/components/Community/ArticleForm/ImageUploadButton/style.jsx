import styled from "styled-components";

const Wrapper = styled.div`
  & .input-footer {
    /*  position: fixed; */
    bottom: 0%;
    padding-top: 25vh;
    margin-bottom: 18vh;
  }

  & .CameraIcon {
    font-size: 50vw;
  }
`;
export default Wrapper;
