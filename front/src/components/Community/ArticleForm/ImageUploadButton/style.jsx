import styled from "styled-components";

const Wrapper = styled.div`
  & .input-footer {
    /*  position: fixed; */
    bottom: 0%;
    margin-top: 180px;
  }

  & .CameraIcon {
    font-size: 50vw;
  }
`;
export default Wrapper;
